-- Flyway Migration: Initial Schema
-- Version: 1.0.0
-- Description: Initial database schema for Cloud Kitchen RBAC Service

-- Note: This is a placeholder migration file
-- In production, you should generate actual migration scripts from your JPA entities
-- or use Flyway's baseline feature if database already exists

-- Example migration structure:
-- CREATE TABLE IF NOT EXISTS schema_version (
--     installed_rank INT NOT NULL,
--     version VARCHAR(50),
--     description VARCHAR(200) NOT NULL,
--     type VARCHAR(20) NOT NULL,
--     script VARCHAR(1000) NOT NULL,
--     checksum INT,
--     installed_by VARCHAR(100) NOT NULL,
--     installed_on TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
--     execution_time INT NOT NULL,
--     success BOOLEAN NOT NULL
-- );

-- For existing databases, use: mvn flyway:baseline
-- For new databases, migrations will run automatically on startup

