server.address=0.0.0.0
server.port=8081

# --- Database ---
spring.datasource.url=jdbc:postgresql://localhost:5432/cloud_kitchen_rbac
spring.datasource.username=postgres
spring.datasource.password=Yogesh@1234
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# --- SQL Initialization ---
spring.sql.init.mode=never

# --- JWT ---
app.jwt.secret=q2M7w1SiV5XDAXEmVqg9gZWCXfF50l11FqJR2aSC1a8=
app.jwt.access-valid-seconds=86400
app.jwt.refresh-valid-seconds=604800
app.jwt.expiration=86400000

# --- Swagger / Springdoc ---
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true