# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/cloud_kitchen_rbac
DB_USERNAME=postgres
DB_PASSWORD=Yogesh@1234

# JWT Configuration (REQUIRED for production - minimum 32 characters)
# Generate secure secret: openssl rand -base64 32
JWT_SECRET=2M7w1SiV5XDAXEmVqg9gZWCXfF50l11FqJR2aSC1a8K9mN3pQ7rT2uV8wX1yZ4bC6dE9fG2hJ5kL8mN1oP4qR7sT3uV6wX9yZ2
JWT_ACCESS_EXPIRY=86400
JWT_REFRESH_EXPIRY=604800

# CORS Configuration
# Development: Use * to allow all origins
# Production: Set specific origins (comma-separated) - NEVER use * in production
# Example: CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# AWS S3 Configuration (MANDATORY)
# IMPORTANT: Replace placeholders with your actual AWS credentials
# NEVER commit real credentials to git - use .env file locally (already in .gitignore)
AWS_ACCESS_KEY=<your-aws-access-key-here>
AWS_SECRET_KEY=<your-aws-secret-key-here>
AWS_REGION=us-east-1
AWS_S3_BUCKET=<your-s3-bucket-name>
AWS_S3_ENDPOINT=

# Application Configuration
PORT=8081
SPRING_PROFILES_ACTIVE=prod

# DevTools - Auto Restart (Development Only - Set to false in production)
DEVTOOLS_ENABLED=false
